import React from 'react';
import { Container, TextTitle } from './styles';

const Favorites = () => {
    return (
        <Container>
            <TextTitle>Favorites</TextTitle>
        </Container>
    );
}

export default Favorites;
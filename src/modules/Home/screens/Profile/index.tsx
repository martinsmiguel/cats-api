import React from 'react';
import { Container, TextTitle } from '../Favorites/styles';

const Profile = () => {
    return (
        <Container>
            <TextTitle>Profile</TextTitle>
        </Container>
    );
}

export default Profile;